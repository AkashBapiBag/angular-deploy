version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - node -v
      - npm -v
      - npm install -g @angular/cli
      - npm install
  build:
    commands:
      - ng build --configuration production
      - ls -lrat
      - cp deploy.yml dist/angular-deploy/browser/

artifacts:
  files:
    - '**/*'
  base-directory: dist/angular-deploy/browser
